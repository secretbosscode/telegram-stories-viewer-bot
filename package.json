{
  "name": "telegram-stories-viewer-bot",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "author": "k.khamidov <khamidoff09@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "build": "tsc && tsc-alias",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "process:stop": "npx pm2 stop ecosystem.config.js && npx pm2 delete ecosystem.config.js",
    "process:start": "yarn build && npx pm2 start ecosystem.config.js",
    "deploy": "yarn process:stop && yarn process:start"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.3",       // latest as of June 2025
    "better-sqlite3": "^9.5.3",               // latest
    "body-parser": "^1.20.3",                 // latest
    "dotenv": "^16.4.3",                      // latest
    "effector": "^23.4.0",                    // latest
    "express": "^4.19.2",                     // latest 4.x, 5.x is still in beta
    "input": "^1.0.2",                        // latest
    "patronum": "^2.4.0",                     // latest
    "stripe": "^13.16.0",                     // latest
    "telegraf": "^4.17.0",                    // latest (still 4.x, 5.x is not released/stable)
    "telegram": "2.19.11"                     // latest
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",        // latest
    "@types/body-parser": "^1.19.4",          // latest
    "@types/express": "^4.17.24",             // latest
    "@types/node": "^20.12.13",               // latest
    "eslint": "^9.1.0",                       // latest (major version bump, update config as needed)
    "eslint-kit": "^9.2.0",                   // latest
    "nodemon": "^3.1.0",                      // latest
    "prettier": "^3.2.5",                     // latest
    "ts-node": "^10.9.2",                     // latest (v10 is still fine for now)
    "tsc-alias": "^1.9.11",                   // latest
    "tsconfig-paths": "^4.3.2",               // latest
    "typescript": "^5.4.5"                    // latest
  }
}
